.env

# Local identity overrides (never commit)
git/.gitconfig.local
git/.work-gitconfig.local

# SSH: ignore everything by default
ssh/.ssh/**

# Allow only safe tracked SSH files
!ssh/.ssh/config
!ssh/.ssh/*.pub
!ssh/.ssh/**/*.pub
